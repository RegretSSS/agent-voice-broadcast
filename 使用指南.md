# 使用指南

支持 **Windows / macOS / Linux / WSL2** 开箱即用。同一份代码、同一套 hooks 配置范式，三个平台只是 Python 解释器与后台启动方式不同。

## 当前版本做了什么

- 基于现成 edge-tts 语音包的流式分句播放
- 常驻 TTS 服务（系统托盘控制 + Stop hook 自动播报）
- daemon 自动去重（request_id + 内容哈希）

## 最常用命令

```bash
python sci_fi_tts.py
python sci_fi_tts.py say "系统已就绪。"
python sci_fi_tts.py say-file sample_story.txt
python sci_fi_tts.py list-voices
```

## 说明

- 现在优先追求更快开口
- 长文本会自动分句
- 每一句生成完成后就会尽快播放

---

## 安装（按平台）

### Windows

```powershell
pip install -r requirements.txt
```

### macOS

pygame 在 Apple Silicon 上需要 `SDL2` 与 `SDL2_mixer`，建议先装 brew：

```bash
brew install sdl2 sdl2_mixer
pip install -r requirements.txt
```

> pygame 2.5+ 的 wheel 已经在大多数 mac 上自带 SDL；如果 `pip install` 后跑得起来就不用 brew。

### Linux / WSL2

```bash
sudo apt update
sudo apt install -y python3-pip libsdl2-mixer-2.0-0 \
                    gir1.2-ayatanaappindicator3-0.1 || true
pip3 install -r requirements.txt
```

> WSL2 没有桌面环境时，pystray 托盘不会显示，但 daemon 仍可作为后台服务正常播报（Windows 端托盘可见，WSL 端只发请求）。

---

## 常驻服务

### 是什么

后台跑一个 `tts_daemon.py`，监听 `0.0.0.0:48271`：

- 单 `pygame.mixer` 实例 → 多条播报**天然不重叠**
- `asyncio.Queue` 单消费者 → **严格 FIFO 顺序播放**
- 系统托盘图标 → 鼠标右键可暂停/继续/跳过/清空/退出（macOS 显示在菜单栏；Linux 取决于桌面环境；Windows 在系统托盘）
- 绑 `0.0.0.0` 是为了让 WSL2 NAT 模式能经 Windows 主机 IP 连进来

### 第一次启动

**不需要手动启动**。Stop hook 第一次触发时，`claude_hook_bridge.py` 会按当前平台自动用合适方式后台拉起 daemon：

| 平台    | 拉起方式                                              |
|---------|-------------------------------------------------------|
| Windows | `pythonw.exe` + `DETACHED_PROCESS` + `CREATE_NO_WINDOW` |
| macOS   | `python3` + `start_new_session=True`（setsid 脱离 tty） |
| Linux   | `python3` + `start_new_session=True`                  |
| WSL2    | 不直接拉起，先打 Windows 主机 IP；连不上再拉本机 daemon |

### 手动启动 / 调试

```bash
# 任意平台，前台跑（看日志输出）
python tts_daemon.py

# Windows 后台
pythonw tts_daemon.py

# macOS / Linux 后台
nohup python3 tts_daemon.py >/dev/null 2>&1 &
```

### 日志位置

| 平台    | 路径                                                  |
|---------|-------------------------------------------------------|
| Windows | `%LOCALAPPDATA%\tts_daemon.log`                       |
| macOS   | `~/Library/Logs/tts_daemon.log`                       |
| Linux   | `~/.local/share/tts-daemon/tts_daemon.log`（遵循 XDG_STATE_HOME） |

### 系统托盘菜单

右键托盘图标（macOS 是菜单栏左键）：

- **暂停** / **继续**：暂停或恢复当前播放和队列
- **跳过当前**：跳过正在播的句子，队列继续
- **清空队列**：丢弃所有未播报的内容
- **退出**：关闭 daemon（下次 hook 触发会自动重启）

### Stop hook 自动播报

在你的 Claude Code `settings.json` 里加 hooks。**不要硬编码本机路径**——用环境变量或者你自己的实际路径替换 `<项目根>`：

```jsonc
{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            // Windows
            "command": "python \"<项目根>\\claude_hook_bridge.py\""
            // macOS / Linux
            // "command": "python3 \"<项目根>/claude_hook_bridge.py\""
          }
        ]
      }
    ]
  }
}
```

`--source` 不指定时按平台自动推断：

| 平台    | 默认 source             |
|---------|-------------------------|
| Windows | `ClaudeCodeInWindows`   |
| macOS   | `ClaudeCodeInMacOS`     |
| Linux   | `ClaudeCodeInLinux`     |
| WSL2    | `ClaudeCodeInLinux`     |

想手动覆盖就加 `--source ClaudeCodeInXXX`。

每次 Claude Code 结束响应时，bridge 会：

1. 解析 transcript 拿到最后一条 user 消息（任务）和 assistant 消息（结果）
2. 用 `md_to_speech.py` 清洗 markdown（代码块 → "略过详细代码"，表格 → 表头+第一列+行数）
3. 拼成播报文案：`<source> 报告。任务：…。完成情况：…。`
4. POST 给 daemon 加入队列

### WSL 端身份

WSL 那边的 Claude Code 直接调用同一份 `claude_hook_bridge.py`（从 WSL 看，项目通常克隆在 `/mnt/.../agent-voice-broadcast/`）。bridge 会自动检测 `/proc/version` 里的 microsoft 标记，从 `/etc/resolv.conf` 的 nameserver 拿 Windows 主机 IP，串到同一个 Windows daemon 上。连不上才回退本机 Linux daemon。

```jsonc
// WSL 端 ~/.claude/settings.json
{
  "hooks": {
    "Stop": [{
      "matcher": "",
      "hooks": [{
        "type": "command",
        "command": "python3 \"/mnt/.../agent-voice-broadcast/claude_hook_bridge.py\""
      }]
    }]
  }
}
```

> 历史 `~/.claude/hooks/tts_stop.py` 的能力已合并进 `claude_hook_bridge.py`，可以直接删除旧脚本。

### Markdown 清洗规则（md_to_speech.py）

| 输入 | 输出 |
|------|------|
| ` ``` fenced code block ``` ` | "略过详细代码。" |
| `` `inline code` `` | 保留原文（去掉反引号） |
| markdown 表格 | "表头：A、B。第一列为：X、Y。共 N 行。" |
| 表格行数 > 5 | 只报 "表头：…。共 N 行。" |
| `## 标题` / `**粗体**` | 去掉装饰符号 |
| `[text](url)` / `![alt](url)` | 前者留 text，后者删除 |

### API 端点（手动测试）

```bash
# ping
curl http://127.0.0.1:48271/ping

# 加一条播报
curl -X POST http://127.0.0.1:48271/say \
  -H "Content-Type: application/json" \
  -d '{"source":"test","text":"测试播报。"}'

# 状态
curl http://127.0.0.1:48271/status

# 跳过 / 清空 / 暂停 / 继续 / 关闭
curl -X POST http://127.0.0.1:48271/skip
curl -X POST http://127.0.0.1:48271/clear
curl -X POST http://127.0.0.1:48271/pause
curl -X POST http://127.0.0.1:48271/resume
curl -X POST http://127.0.0.1:48271/shutdown
```
